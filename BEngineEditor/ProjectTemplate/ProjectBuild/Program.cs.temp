using BEngineCore;

namespace ProjectBuild
{
	internal class Program
	{
		static void Main(string[] args)
		{
			Scripting scripting = new Scripting();
			scripting.Initialize(Path.Combine(Directory.GetCurrentDirectory(), "ProjectAssembly.dll"));

			Window window = new();
			window.Run();
		}
	}
}
